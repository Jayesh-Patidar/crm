import { Link } from "@mui/material";
import { FC } from "react"

const Sidebar: FC = ({ routes }) => {

    return <div>
        {/* {routes.map(route => <Link href={route.})} */}
    </div>
}

export default Sidebar;